# Review Checklist

## 功能检查

- 用户可以打开网页。
- 用户可以创建 Passkey。
- 用户可以创建测试网钱包。
- 用户可以用 Passkey 解锁钱包。
- 用户可以看到 Ethereum、BSC、Bitcoin、Solana、TRON 五条测试网地址。
- 每个地址都可以复制。
- 每个地址都有对应二维码。
- 用户可以查看五条链原生币余额。
- 用户可以完成 Ethereum Sepolia 测试转账。
- 用户最终可以完成五条链原生币测试网转账。
- 广播成功后显示 tx hash。
- 广播成功后显示 explorer 链接。
- 用户可以退出当前会话。
- 用户可以重新用 Passkey 解锁。
- 用户可以导出助记词备份。

## 页面检查

### 创建页

- 页面显示 `Create Passkey`。
- 页面说明这是无助记词日常体验。
- 页面说明使用生物识别或系统 Passkey。
- 不支持 Passkey 时显示清楚原因。

### Dashboard

- 显示总资产。
- 显示五条链的地址状态。
- 显示快捷入口：Receive、Send、Portfolio、Settings。
- 空资产状态看起来正常，不像页面坏了。

### Portfolio

- 显示总资产价值。
- 显示资产分布。
- 显示最近活动。
- 没有交易时显示空状态。

### Deposit Funds

- 可以选择网络。
- 切换网络后地址和二维码同步更新。
- 地址可以复制。
- 有网络风险提示。
- Ethereum 和 BSC 页面提醒地址可能相同但网络不同。
- TRON 页面提醒账户激活、带宽和能量。

### Send Funds

- 可以选择网络和原生币。
- 可以输入收款地址和金额。
- 地址格式错误时不能继续。
- 余额不足时不能继续。
- 手续费不足时不能继续。
- 签名前显示转账摘要。
- 广播前要求用户二次确认。

### Settings

- 显示当前 Passkey 状态。
- 显示备份状态。
- 可以进入导出助记词备份流程。
- 危险操作有明显提示。

## 错误处理检查

- 浏览器不支持 Passkey 时，有友好提示。
- WebAuthn PRF 不支持时，不继续创建钱包。
- Token Core WASM 加载失败时，有友好提示。
- RPC 请求失败时，页面不崩溃。
- 余额加载失败时，可以重试。
- 地址格式错误时，提示用户检查地址。
- 转账金额为 0 时不能提交。
- 转账金额超过余额时不能提交。
- 用户取消 Passkey 时，提示“已取消”，不要报错吓人。
- 广播失败时显示失败原因和重试建议。
- 网络切错时有明确提醒。

## 钱包安全检查

- 第一版只支持测试网。
- 页面没有主网广播入口。
- 默认不显示助记词。
- 默认不显示私钥。
- 默认不显示 root seed。
- 控制台不打印助记词。
- 控制台不打印私钥。
- 控制台不打印 Passkey PRF key。
- 日志和错误上报不包含钱包秘密。
- 私钥和助记词不进入 React 全局状态。
- 钱包秘密不放进 URL。
- 钱包秘密不放进 localStorage。
- 导出助记词前必须 Passkey 解锁。
- 导出助记词前必须显示安全提示。
- 签名前必须展示人能看懂的交易摘要。
- 广播前必须二次确认。
- 后端不得保存明文助记词、私钥、PRF key 或 root seed。

## 链专项检查

### Ethereum / BSC

- Ethereum 和 BSC 使用正确 chainId。
- Ethereum 和 BSC RPC 分开配置。
- Ethereum 和 BSC explorer 链接正确。
- 页面提醒 EVM 地址相同不代表网络相同。

### Bitcoin

- 使用测试网或 Signet 地址。
- 转账前展示手续费。
- 转账前展示找零说明。
- 不把 BTC 地址和 EVM 地址混用。

### Solana

- 使用 Solana Devnet。
- 地址格式正确。
- SOL 转账前显示手续费。
- Solana 私钥不进入日志。

### TRON

- 使用 TRON 测试网。
- 地址以 `T` 开头。
- TRX 转账前提示带宽、能量或账户激活问题。
- 不把 TRON 地址和 EVM 地址混用。

## 部署检查

- 部署地址必须是 HTTPS。
- Passkey 在部署域名下可以正常创建。
- `rpId` 与部署域名匹配。
- 页面刷新后不会丢失加密钱包数据。
- 没有把正式 RPC API Key 打包进前端。
- `.env` 不提交到 Git。
- `.env.example` 只放占位符。
- 构建命令能成功运行。
- 部署后页面能打开。
- 部署后创建钱包流程能跑通。

## 最终验收

最终验收通过的标准：

用户在 PC 浏览器打开部署网站后，可以创建 Passkey 钱包，查看五条测试网地址，复制收款地址，查看原生币余额，完成测试网转账，看到 tx hash，并能导出助记词备份。

